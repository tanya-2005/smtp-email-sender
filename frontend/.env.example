# Local development: points at the backend running on your machine.
# In production (Vercel, etc.), set this to your deployed backend's URL,
# e.g. https://your-backend.up.railway.app/api - it must be set at build
# time since Vite inlines VITE_ vars into the production bundle.
VITE_API_BASE_URL=http://localhost:3000/api
