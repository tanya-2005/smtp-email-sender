PORT=3000
NODE_ENV=development

# Email is not configured here. Use the Sender Account page in the app
# (backed by POST /api/settings), which stores your sender details and
# Resend API key in backend/data/settings.json.
