PORT=3000
NODE_ENV=development

# SMTP credentials are no longer configured here. Use the SMTP Settings
# page in the app (backed by POST /api/settings), which stores your
# provider, sender details, and password in backend/data/settings.json.
